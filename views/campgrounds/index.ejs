<% layout('layouts/boilerplate')%>
    <h1>All Listed Campgrounds</h1>
    <div>
        <a href='/campgrounds/new'>Add Campgrounds</a>
    </div>
    <ul>
        <% for (let campground of campgrounds) {%>
            <li><a href='/campgrounds/<%= campground._id%>'</a><%= campground.title %></li>
            <% }%>
    </ul>
